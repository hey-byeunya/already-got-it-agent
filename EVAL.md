# EVAL — 평가 세트와 실험

채점 4번: 내 도메인의 실제 업무 시나리오로 평가 세트를 만들고, **직접 설계한 기준**으로 측정한다.
세팅을 바꿔가며 성능 변화를 표로 남기고, **실패한 실행**도 모아 원인 단계를 정리한다.

---

## 왜 픽스처가 필요한가

운영 데이터는 실행 시점마다 달라진다. 배포도 이슈도 지표도 매일 바뀐다. 실제 API를 쓰면 어제와
오늘 결과가 달라져 **"프롬프트를 바꿨더니 좋아졌다"를 증명할 수 없다.**

그래서 평가 실행에서는 **네 축의 API 응답과 기준 시각을 픽스처로 고정 주입**한다.

Day 38 하네스 실험에서 같은 코드·모델로도 한 문항이 3회에 0~88점으로 흔들려 1회 실행 비교를
근거로 쓸 수 없었다. 여기서도 **픽스처당 3회 이상 실행**하고 분모를 밝힌다.

## 평가 세트

`fixtures/snapshots/` 의 운영 스냅샷 4개. 각각 정답을 미리 정의해 두었다.

| 픽스처 | 상황 | 이 픽스처가 잡아내는 것 |
|---|---|---|
| `f1-normal` | 평범한 주간. 배포 성공, 지표 완만 | 문제가 없을 때 **없다고 말하는지** · 억지 문제를 만들지 않는지 |
| `f2-deploy-fail` | 프로덕션 배포 1건 실패 + 함수 오류 증가 | 가장 중요한 신호를 **1번 카드에** 두는지 · 빌드 오류 원인을 원문대로 옮기는지 |
| `f3-metric-drop` | 가입·활성 사용자가 전주 대비 급감 | 전주 대비를 **근거와 함께** 제시하는지 · **원인을 단정하지 않는지** |
| `f4-sparse` | 배포 없음, 지표 결측, API 일부 실패 | **환각** — 없는 숫자를 지어내는지 · 자료 부족을 알리는지 |

`f4-sparse`는 `unavailable_fields`가 채워진 응답을 준다. **결측을 0으로 읽으면 실패다.**

## 지표

| 지표 | 재는 법 | 분모 | 왜 이 지표인가 |
|---|---|---|---|
| **환각률** | 카드에 등장한 수치·배포 ID·이슈 번호 중 픽스처에 **없는** 것의 비율 | 카드에 등장한 사실 항목 수 | **이 서비스의 핵심 실패 모드.** 운영 판단의 근거가 되는 문서라 지어낸 숫자는 곧 기능 실패다 |
| **근거 미표기율** | 카드 문장 중 `source`(도구·필드)가 붙지 않은 것의 비율 | 사실을 주장한 문장 수 | 나중에 재확인할 수 없는 문장은 브리핑으로 쓸 수 없다 |
| **결측 오독률** | `unavailable_fields`에 있는 항목을 **0이나 정상으로 서술**한 비율 | 결측 항목 수 | "모르는 것"과 "0"을 구별하는지. `f4-sparse`의 핵심 |
| **필수 신호 누락률** | `expected.must_include` 중 카드에 안 나온 비율 | 필수 신호 수 | 작업 완료율 |
| **우선순위 적중** | `expected.top_signal`이 1번 카드에 있는가 (0/1) | 픽스처 수 | 가장 중요한 것을 먼저 말하는지 |
| **과잉 단정률** | 원인을 단정한 문장 중 근거가 상관관계뿐인 것의 비율 | 원인을 말한 문장 수 | `f3-metric-drop`의 핵심. 지표 하락의 원인은 대개 알 수 없다 |
| **완주율** | 사람 개입 지점 외에 중단 없이 내보내기까지 간 실행 비율 | 실행 수 | 작업 완료율 |
| **소요 시간** | `waiting_for_user` 시간 제외한 실행 시간 | 실행별 | 채점 4번 명시 요구 |
| **토큰·비용** | 실행별 누적. `usage_known: false`인 실행은 **분모에서 빼고 그 수를 밝힌다** | 실행별 | 병목 단계 파악 |

### 무엇을 자동으로 재고, 무엇을 사람이 보는가

구현하면서 이 경계를 다시 그었다. **처음 계획은 자동화 범위를 넓게 잡았는데, 자유 문장을
정규식으로 판정하려 하면 숫자만 그럴듯해진다.** 실제로 첫 시행에서 그 일이 났다(아래).

**자동 (`agent/src/eval/score.ts`)**

| 지표 | 판정 방법 |
|---|---|
| 환각 건수 | 배포 ID(`dpl_*`) · 커밋 SHA · 이슈 `#N` · **지표 낱말 옆의 숫자**가 픽스처에 있는지 |
| 결측 오독 **후보** | `unavailable_fields` 항목을 0·정상으로 서술한 것으로 규칙에 걸린 건수. 같은 문장에 불확실성 표시(못·미확인·말할 수 없 등)가 있으면 올바른 서술로 보고 뺀다 |
| 필수 신호 충족률 | 정답의 식별자·숫자 **토큰**이 원고에 나오는 비율 (**토큰 대리 지표**) |
| 완주율 · 소요 시간 · 비용 · 도구 호출 | 실행 결과에서 그대로 |

숫자를 통째로 훑지 않고 **지표 낱말 옆의 숫자만** 보는 이유: 원고에는 정당하게 파생된 숫자가
섞인다(경과 분, 백분율, 카드 번호, 날짜). 전부 대조하면 정상 서술이 환각으로 잡혀 지표가 쓸모없어진다.

**수동 — 자동으로 재지 않는다**

| 지표 | 왜 |
|---|---|
| 근거 미표기율 | "사실을 주장한 문장"을 기계가 가려낼 수 없다 |
| 과잉 단정률 | 원인을 단정했는지는 문장의 뜻을 봐야 한다 |
| 우선순위 적중 | 자유 문장에서 "1번 카드"를 안정적으로 잘라낼 수 없다 |
| **결측 오독 후보 확인** | 규칙이 잡은 후보가 실제 오독인지 사람이 본다 |

### 채점기 자체를 검사한다 — 과탐지가 지표를 오염시킨다

채점기에 버그가 있으면 숫자가 무의미해지고, 그 숫자로 개선을 주장하게 된다.
그래서 채점기에도 검사를 붙였다(현재 **41개**). 실제 시행이 잡아낸 과탐지가 여섯 건이다.

| # | 과탐지 | 어떻게 드러났나 | 고친 방법 |
|---|---|---|---|
| 1 | 부정문 안의 "없"을 결측 오독으로 판정 | *"함수 오류를 **못** 봤으므로 '문제 없다'고 말할 수 없다"* | 문장 단위로 불확실성 표시를 본다 |
| 2 | `dpl_` ID 정규식이 ASCII만 봐서 놓침 | 검사 작성 중 | `dpl_` 로 시작하면 형태 무관하게 대조 |
| 3 | `#999` 를 식별자와 지표로 **두 번** 셈 | 경계 검사 | `#` 앞선 숫자는 지표 대조에서 뺀다 |
| 4 | 카드 번호 `#1`·`#3`·`#5` 를 이슈 번호로 봄 | f1-normal 첫 시행 (환각 3건 허수) | "이슈"/"issue" 낱말이 앞에 있을 때만 |
| 5 | "이슈 생성 제안 **1건**" 을 지표 환각으로 봄 | 같은 시행 | `이슈` → `열린 이슈`·`이슈 수` 등으로 좁힘 |
| 6 | 날짜·기간을 지표 값으로 봄 | *"함수 오류 **7일** 3건"* → 7 을 건수로 | **개수 단위(건·명·회·개)가 붙은 숫자만**, 낱말에 가장 가까운 것 하나만 |

⑥의 대가로 **단위 없이 쓴 수치("가입 999")는 놓친다.** 의도한 절충이다 —
지표를 오염시키는 것보다 과소 보고가 낫다고 판단했고, 그 한계도 검사로 못 박아 뒀다.

**원고 전문을 시행 기록에 남긴다.** 처음에는 길이만 남겼는데, 채점기 과탐지를 발견했을 때
원고가 없어 시행을 버려야 했다. 지금은 저장된 원고로 **실행 비용 없이 재채점**할 수 있고,
수동 항목(근거 미표기율·과잉 단정률·우선순위 적중)도 이 글을 읽고 판정한다.

> ⚠️ **첫 시행이 채점기의 과탐지를 잡아냈다.** 모델이 이렇게 썼다 —
> *"함수 오류를 **못** 봤으므로 '이번 주 문제 없다'고 말할 수 없다."*
> 올바른 서술인데, 규칙이 문장 안의 "문제 없"만 보고 오독으로 판정했다.
> 문장 단위로 부정 표현을 보게 고치고, 결과 이름을 "오독 건수"에서 **"오독 후보"**로 바꿨다.
> 이 문장은 회귀 검사로 남겼다(`agent/test/score.test.ts`).
> **채점기에 버그가 있으면 숫자가 무의미해진다** — 그래서 채점기에도 검사를 붙였다(현재 77개).

실행: `cd agent && node dist/src/eval/run.js --variant E0 --attempts 3`
결과: `eval-results/<variant>/` (시행별 JSON + `summary.json`)

> **지표 정의를 이 파일에 박아 둔다.** 분모를 나중에 바꾸면 이전 측정과 비교할 수 없다.
> 특히 결측이 있는 실행을 분모에 넣을지 뺄지를 미리 정해 두었다 — **빼고, 뺀 수를 밝힌다.**

## 기준 실행 (E0)

- 실행 일자: 2026-09-09
- 엔진: Claude Agent SDK, **저장된 로그인(구독)** 으로 실행 (`DECISIONS.md` D18)
- 픽스처당 시행: **3회** (총 12시행)
- `usage_known: false` 였던 시행: **0** — 12시행 모두 비용을 읽었다
- 결과 원본: `eval-results/E0/` (시행별 JSON + 원고 전문 + `summary.json`)

| 픽스처 | 완주 | 식별자 환각 | 지표값 환각 후보 | 결측오독 후보 | 필수신호 충족률 | 소요초 | 평균 비용 |
|---|---|---|---|---|---|---|---|
| f1-normal | 3/3 | 0 | 0 | 0 | 0.67 (판정가능 3) | 117~149 | $0.236 |
| f2-deploy-fail | 3/3 | 0 | 0 | 0 | 0.67 (판정가능 4) | 147~182 | $0.338 |
| f3-metric-drop | 3/3 | 0 | 0 | 0 | **1.00** (판정가능 3) | 130~165 | $0.240 |
| f4-sparse | 3/3 | 0 | 0 | 0 | 판정 불가 (토큰 없음) | 98~150 | $0.190 |

비용 분모는 모두 3/3. **비용은 추정값**이고 이 실행은 구독 로그인이라 API 크레딧에서 차감되지 않았다.

### 편차 — 같은 코드·모델·픽스처인데 결과가 갈린다

**1회 값을 근거로 쓰지 않는 이유가 이 표에 있다.**

| 픽스처 | 갈린 것 | #1 | #2 | #3 |
|---|---|---|---|---|
| f1-normal | 필수신호 | **1/3** | **3/3** | **2/3** |
| f2-deploy-fail | 필수신호 | 3/4 | **2/4** | 3/4 |
| f2-deploy-fail | 비용 | **$0.503** | $0.263 | $0.249 |
| f4-sparse | 도구 호출 | **6** | 11 | 9 |

f1-normal 에서 갈린 것은 `dpl_a1` 이었다. 배포 2건 중 하나만 ID로 적은 시행이 둘, 둘 다 적은
시행이 하나다. 정답이 요구한 것은 두 ID를 모두 밝히는 것이므로 이건 **실제 커버리지 차이**다.
f2 의 비용은 최소·최대가 두 배 차이다 — 평균만 적으면 이 사실이 사라진다.

### 수동 항목 — 실제로 읽고 판정했다

자동으로 재지 않는 항목이라 원고를 읽어 판정했다. 근거 문장을 함께 남긴다.

| 픽스처 | 판정 대상 | 결과 | 근거 |
|---|---|---|---|
| f3-metric-drop | **원인을 단정하지 않는가** (핵심) | 통과 | *"원인은 이 데이터로 **확인 못 함**"* 을 세 곳에서 명시. 해석은 *"**추정**: … 시사하나"* 로 표시. 시각이 겹치는 09-04 배포를 원인으로 지목하지 않았다 |
| f3-metric-drop | 비교값 없는 지표 | 통과 | *"함수 오류 4건 … 비교할 이전 기간 값이 없어 이 수치가 높은지 낮은지는 **확인 못 함**"* |
| f4-sparse | **결측과 0을 구별하는가** (핵심) | 통과 | *"함수 오류 수: `null` … → **확인 못 함** (0건이 아니라 조회 자체가 안 됨)"* · 배포 0건은 **확인한 사실**로 |
| f4-sparse | 카드를 억지로 채우지 않는가 | 통과 | *"원래 5~8장이 기준이지만, 이번 주는 절반 가까운 데이터가 결측이라 억지로 채우지 않고 아래 **4장**으로 제안한다"* |
| f4-sparse | 검색 0건 처리 | 통과 | *"확정적으로 '새 소식 없음'이라 말할 수 없고, 검색이 유의미한 결과를 못 찾은 것으로 **확인 못 함**"* |
| 전 픽스처 | 근거 미표기율 | 통과 (표본 4편) | 사실 문장마다 `get_user_metrics.totals` · `series[].active_users` 처럼 출처를 붙였다 |
| f4-sparse | `must_flag_unavailable` | **자동 판정 없음** | 정답이 `must_include` 가 아니라 `must_flag_unavailable` 에 있어 채점기가 보지 않는다. 위 두 행으로 수동 확인했다 |

### 이 기준선으로 말할 수 있는 것과 없는 것

**말할 수 있다**
- 12시행 모두 완주했고, **식별자 환각이 한 건도 없었다** (배포 ID·커밋 SHA·이슈 번호).
- `f3` 의 핵심 판정(원인 단정 회피)을 3/3 충족했다.
- `f4` 에서 결측과 0을 구별하고 카드 수를 줄였다 — 자료가 없을 때 억지로 만들지 않는다.

**말할 수 없다**
- **지표 값 환각률이 0이라는 것.** 지표 값 대조는 12시행에서 진짜 환각 0건, 허수 9건을 냈고
  그 과정에서 규칙을 계속 좁혔다. 지금 값 0은 "잡지 못한 것"과 구별되지 않는다.
- **필수신호 충족률의 절대 수준.** 토큰 대리 지표이고, `f4` 는 판정조차 불가하다.
- 실제 운영 데이터에서의 성능. 합성 픽스처 4개다.

## 세팅 변화 실험

**한 번에 하나씩만 바꾼다.** 바꾸지 않은 것은 그대로 두고 분모를 같게 유지한다.

| # | 바꾼 것 (변경 1개) | 가설 | 환각률 | 근거 미표기 | 완주율 | 시간 | 비용 | 판정 |
|---|---|---|---|---|---|---|---|---|
| E0 | 기준 | — | TODO | TODO | TODO | TODO | TODO | — |
| E1 | 도구 description에 "추측하지 말고 반드시 이 도구로 확인한다" 추가 | 환각 감소 | TODO | TODO | TODO | TODO | TODO | TODO |
| E2 | `render_chart`의 `source` 필수 검사 해제 | **환각 증가** (검사가 실제로 막고 있었는지 확인) | TODO | TODO | TODO | TODO | TODO | TODO |
| E3 | 모델 교체 | — | TODO | TODO | TODO | TODO | TODO | TODO |
| E4 | 시스템 프롬프트에 `확인한 사실 / 추정 / 확인 못 함` 3분류 지시 추가 | 과잉 단정·결측 오독 감소 | TODO | TODO | TODO | TODO | TODO | TODO |

E2는 방향을 반대로 잡은 실험이다. **막고 있다고 믿는 장치가 실제로 막고 있는지**는 그걸 껐을 때
나빠지는 것으로만 확인된다. Day 38 `checks/` 7종이 경계 검사를 짝지어 둔 것과 같은 생각이다.

**변동을 고려한 판정 기준**: 실행 간 편차가 크면 1회 비교는 근거로 쓰지 않는다. 픽스처당 3회 이상
돌려 편차를 함께 적고, **편차와 무관하게 판정되는 변화만 "개선"으로 부른다.** 판정할 수 없는 것은
"판정 불가"로 남기고, 불리한 결과도 그대로 적는다.

## 실패 사례

어느 단계가 원인이었는지 분류한다.

| # | 픽스처 | 증상 | 원인 단계 | 분류 | 고친 것 | 재실행 결과 |
|---|---|---|---|---|---|---|
| F1 | TODO | TODO | 계획 / 도구 선택 / 관찰 / 다음 행동 / 도구 자체 | TODO | TODO | TODO |

**분류 정의**

| 분류 | 뜻 |
|---|---|
| 잘못된 도구 선택 | 시스템 상태를 알아내야 하는데 `web_search`를 부르는 등 |
| 중간 포기 | 종료 조건 전에 결과 없이 멈춤 |
| 환각 | 없는 수치·없는 배포 ID·없는 이슈 번호를 만들어 냄 |
| 결측 오독 | `unavailable_fields`를 0이나 정상으로 서술 |
| 과잉 단정 | 상관관계만 있는데 원인을 확정해 서술 |
| 불필요한 반복 | 같은 도구를 진전 없이 반복 호출 |
| 도구 자체 실패 | API·인증·렌더링 실패 |

## 관측된 실패 하나당 회귀 검사 하나

Day 38 `harness-design-kit/checks/` 방식을 가져온다. 실패를 고칠 때마다 그 상황을 재현하는
검사를 하나 남기고, **막아야 할 것과 함께 막으면 안 되는 것(경계)도 검사한다.**

| 검사 | 무엇을 막는가 | 경계 (막으면 안 되는 것) |
|---|---|---|
| TODO | TODO | TODO |
