# PRD — 「이미 있어」 주간 운영 브리핑 에이전트

「이미 있어」(already-got-it)를 **만들고 운영하는 개발자 본인**을 위해, 시스템 상태 · 사용자 지표 ·
개발 활동 · IT 트렌드 네 축을 읽어 **주간 운영 브리핑 카드뉴스**를 만들어 주는 에이전틱 워크플로 서비스다.

- 대상 앱: https://already-got-it.vercel.app ([hey-byeunya/already-got-it](https://github.com/hey-byeunya/already-got-it))
- 저장소: https://github.com/hey-byeunya/already-got-it-agent
- 실행: 로컬 (`npm run dev`). 결과물과 화면은 캡처해 `evidence/`에 남긴다

---

## 1. 문제 정의

**어떤 반복 업무인가**

앱을 하나 배포해 두면 매주 같은 점검을 반복한다 — 배포가 깨지지 않았는지, 함수가 오류를 내지
않는지, 사용자가 늘고 있는지 줄고 있는지, 열어 둔 이슈가 쌓이고 있는지, 쓰고 있는 라이브러리에
보안 권고가 뜨지 않았는지.

문제는 **이 넷이 서로 다른 화면에 흩어져 있다**는 것이다. Vercel 대시보드, Supabase 테이블,
GitHub 이슈 목록, 그리고 릴리스 노트. 네 곳을 각각 열어야 하고, 각 화면은 숫자를 보여주지만
**"지금 중요한 게 무엇인지"는 말해 주지 않는다.** 그래서 다른 일을 하다 보면 며칠씩 안 열어 보고,
그동안 문제는 아무 소리 없이 지나간다.

> 이 서비스가 겨냥하는 실패는 **틀린 답이 아니라 조용한 실패**다. 오류율이 올라가도 아무도
> 말해 주지 않는 상태를, 매주 한 번 사람이 읽을 수 있는 형태로 바꾸는 것이 목적이다.

**입력**

| 입력 | 출처 | 비고 |
|---|---|---|
| 브리핑 기간 | 사용자 입력 (기본: 최근 7일) | 기준 시각과 시간대를 화면에 표시 |
| 깊게 볼 축 | 사용자 입력 (선택) | 비우면 에이전트가 판단 |
| 배포·빌드·함수 오류 | Vercel REST API | 평가 실행에서는 픽스처로 고정 |
| 사용자 도메인 지표 | Supabase 집계 RPC | **집계값만**. 원시 행·개인정보 없음 |
| 개발 활동 (이슈·커밋·PR) | GitHub API | 본인 저장소만 |
| IT 트렌드 | 웹 검색 | 이 앱이 실제로 쓰는 의존성 기준 |

**결과물**

5~8장의 카드뉴스 한 편. 각 카드는 다음 셋 중 하나를 다룬다.

1. **지금 손봐야 할 것** — 실패한 배포, 올라간 오류율, 막힌 흐름
2. **지켜볼 것** — 지표 추이의 변화. 아직 원인을 단정할 수 없는 신호
3. **알아둘 것** — 의존성 릴리스·보안 권고 중 **이 앱에 실제로 해당하는** 것

카드별 PNG와 전체 ZIP, 그리고 **근거 기록**(어느 도구의 어느 값에서 나온 문장인지)을 함께 내려받는다.

**하지 않는 것**

- 코드를 고치거나 배포를 되돌리지 않는다. 발견을 **이슈로 남기는 것**까지가 범위다.
- 근거 없는 수치를 카드에 넣지 않는다. → 핵심 실패 모드. `EVAL.md`의 **환각률**로 측정한다.
- 승인 없이 GitHub에 아무것도 쓰지 않는다. → `DECISIONS.md` D14의 3중 게이트.

## 2. 타겟 유저

| | 내용 |
|---|---|
| **주 사용자** | 「이미 있어」를 혼자 만들고 배포·운영하는 개발자 (= 저장소 소유자 본인) |
| **상황** | 주간 점검. 다른 과제를 하는 동안 앱 상태를 며칠씩 열어 보지 못한 시점 |
| **왜 직접 안 보는가** | 네 곳을 각각 열어야 하고, 각 화면이 "지금 중요한 게 뭔지"를 판단해 주지 않는다 |
| **성공했다고 느끼는 순간** | 카드를 넘기다 **몰랐던 신호 하나**를 발견하고, 그것을 이슈로 남기는 것 |
| **왜 카드뉴스인가** | 텍스트 로그만으로는 맞는지 틀린지 판단하기 어렵다. 숫자를 차트로 보고 한 장에 메시지 하나만 담으면 훑는 데 1분이면 된다 |

## 3. 워크플로 설계

각 단계에서 **에이전트가 판단할 일**과 **도구가 처리할 일**을 구분한다.

| # | 단계 | 도구가 하는 일 | 에이전트가 판단하는 일 | 사람 |
|---|---|---|---|---|
| 1 | 시작 | — | — | **기간·깊게 볼 축 선택** |
| 2 | 수집 | `get_system_health` `get_user_metrics` `get_dev_activity` | 어느 축을 어떤 순서로 볼지 | — |
| 3 | 신호 추림 | — | 네 축에서 다룰 신호 후보와 그 이유, 확실성 | — |
| 4 | 트렌드 조사 | `web_search` | 이 앱이 실제로 쓰는 의존성에 해당하는 것만 골라내기 | **다룰 트렌드 선택** |
| 5 | 분류·판단 | — | 카드 3분류 배정. **확인한 사실 / 추정 / 확인 못 함** 구분 | — |
| 6 | 스토리보드 | — | 5~8장 구성, 카드별 핵심 문장·근거 출처·차트 계획 | **승인 또는 수정 요청** |
| 7 | 제작 | `render_chart` | 차트 종류·축·강조할 지점 | — |
| 8 | 검수 | — | 환각·누락 자체 점검 | **카드 단위 수정** |
| 9 | 후속 액션 | `create_github_issue` ⚠️ | 어떤 신호를 이슈로 남길지, 제목과 본문 | **승인해야 실행** |
| 10 | 되돌리기 | `revert_issue` ⚠️ | — | **승인해야 실행** |
| 11 | 내보내기 | 파일 생성 | — | **최종 승인** |

1·4·6·8·9·10·11이 사람 개입 지점이다. 이때 상태는 `waiting_for_user`이며 **완료가 아니다.**

## 4. 도구 계획

도구 9개는 `mcp-server/`에 **독립 stdio MCP 서버**로 한 번만 구현하고, 앱이 외부 MCP 서버로 붙여 쓴다
(`DECISIONS.md` D13). 입출력 스키마·description·실패 규칙은 `TOOLS.md`에 있다.

| 도구 | 유형 | 권한 | 실행 전 확인 |
|---|---|---|---|
| `get_system_health` | Vercel REST API | 읽기 전용 토큰, 해당 프로젝트만 | 불필요 |
| `get_user_metrics` | Supabase 집계 RPC | **집계값만**, 원시 행 없음 | 불필요 |
| `get_dev_activity` | GitHub API | 읽기 전용, 본인 저장소만 | 불필요 |
| `web_search` | 웹 검색 | 읽기 전용 | 불필요 |
| `render_chart` | 앱 내부 (SVG) | 외부 호출 없음. **근거 데이터 없이는 거절** | 불필요 |
| `create_github_issue` ⚠️ | GitHub API | **issue 생성만**. 삭제·수정 권한 없음 | **필수** |
| `revert_issue` ⚠️ | GitHub API | **이 실행이 승인 기록으로 만든 이슈만** 닫기 | **필수** |

- 어떤 도구에도 Supabase `service_role` 키를 주지 않는다.
- 삭제 권한은 어떤 도구에도 없다. `revert_issue`는 되돌릴 수 있는 상태 변경(이슈 닫기)만 한다.
- Agent SDK의 내장 도구(`Bash`·`Write`·`Edit` 등)는 `disallowedTools`로 차단해 모델이 보지 못하게 한다.

## 5. 사람 개입 지점

| 지점 | 왜 사람이 정해야 하는가 | 승인 없이 넘어가면 |
|---|---|---|
| ① 기간·축 선택 | 이번 주에 무엇이 궁금한지는 본인만 안다 | 관심 없는 축으로 카드가 채워진다 |
| ④ 트렌드 선택 | 검색 결과 중 무엇이 이 앱에 중요한지는 맥락이 필요하다 | 관계없는 릴리스 소식이 들어간다 |
| ⑥ 스토리보드 승인 | 카드 제작에 시간과 비용이 든다 | 잘못된 구성으로 8장을 다 만든다 |
| ⑧ 카드 수정 | 사실 오류는 운영자가 가장 빨리 안다 | 틀린 수치가 그대로 남는다 |
| ⑨ 이슈 생성 ⚠️ | **저장소에 공개 기록을 남긴다** | 동의 없이 이슈가 쌓인다 |
| ⑩ 되돌리기 ⚠️ | 취소도 기록을 남기는 행위다 | 되돌리기가 또 다른 사고가 된다 |
| ⑪ 최종 승인 | 결과물 확정 | 검수 안 된 파일이 나간다 |

⑨⑩은 되돌리기 어려운 작업이므로 **무엇을 어떻게 쓸지 보여준 뒤** 승인을 받는다. 게이트는 앱 화면이
아니라 **MCP 서버 경계**에 있어, 앱을 우회해도 승인 없이는 실행되지 않는다.

## 6. 핵심 기능(MVP)과 화면 구성

**MVP 기능** (2026-09-10 기준, live-t1에서 전부 동작 확인)

- [x] 기간·축 선택 → 기준 시각·시간대·기간 표시
- [x] 네 축 수집과 신호 후보 표시
- [x] 트렌드 후보 표시와 선택
- [x] 스토리보드 표시·승인·수정 요청 (`AskUserQuestion`으로 승인)
- [x] SVG 차트 렌더링 + 텍스트 레이어 분리 배치
- [x] 카드 단위 수정 (텍스트만 고칠 때 차트 유지 — `other_cards_untouched`)
- [x] 이슈 생성 제안과 **승인 게이트** (제안 2건 거절 기록, `permission_denials`)
- [x] 되돌리기 (승인 기록에 있는 이슈만 — 스모크·gate-approve에서 왕복 확인)
- [x] PNG·ZIP·근거 기록 내려받기 (`export_cardnews`, ZIP 직접 열어 확인)
- [x] **실행 로그(trace) 화면** — 어떤 도구를 왜 호출했고 무엇을 받았는지
- [x] 실행별 소요 시간·토큰·**비용** 표시 (추정값임을 명시, `usage_known` 구별)
- [x] 새로고침·서버 재시작 후 이어가기 (`interrupted` + 재개 경로)
- [x] 픽스처 실행 모드 (평가·캡처 재현용, E0 12시행)

**화면**

계획과 달리 축별 페이지를 나누지 않고 실행 한 페이지에 합쳤다.
`/runs/{id}` 하나가 진행·신호·스토리보드·카드·액션·내보내기를 전부 보여준다.

| 경로 | 화면 | 내용 |
|---|---|---|
| `/` | 시작 | `[ RUNS ]` 지난 실행(상태·결과 한 줄·비용) / `[ NEW RUN ]` 축·엔진·모델·픽스처 / `[ PRESETS ]` 4종 / `[ SAVED ]` 즐겨찾기 |
| `/runs/{id}` | 진행·검토·결과·액션 | 프롬프트 헤더 · `[ PROGRESS ]` 6단계 · `[ AXES ]` 4축 · `[ BUDGET ]↔[ USAGE ]` 계기 · 질문/승인 게이트 · `[ DECIDED BY HUMAN ]` · `[ CARDS ]`/`--raw` · `[ TAIL -F ]` 로그 |

화면은 **터미널 형태**다 (`DECISIONS.md` D25). 이 앱이 보여줘야 하는 것이
상한 대비 소진률·`usage_known:false`·폴링 정지·무효가 된 대기 콜백처럼 계기판에 가까운 값이라,
CLI 은유가 그것들에 각각 자리를 준다. 채점 4번의 관찰 가능성이 이 형태에서 더 선명해진다.

## 7. 미정이었던 것 — 전부 결정됨 (2026-09-10)

| 항목 | 결정 |
|---|---|
| Vercel API로 실제 조회 가능한 지표 범위 | 배포 이력·상태·커밋 SHA는 읽힌다. 빌드 오류 원문은 실패 건만. **함수 오류 수는 조회 경로가 없어** `unavailable_fields`로 내린다 (`DECISIONS.md` D17, live-t1에서 재확인) |
| 카드 비율 | **1080×1350** (4:5) 확정. PNG IHDR로 대조한다 (D22) |
| Supabase 집계 RPC가 돌려줄 지표 목록 | `signups`·`owned_created`·`wish_created`·`active_users` 확정. 정의는 `supabase/ops_metrics.sql` 주석에. SQL Editor 실행 완료 (2026-09-10 집계 반환 확인) |

결정된 항목(실행 엔진·언어·시각물·종료 조건·승인 게이트 구조)은 `DECISIONS.md`에 있다.
