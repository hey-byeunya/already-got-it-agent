# AGENT_LOOP — 에이전트 루프와 상태

채점 3번: 계획 → 도구 호출 → 결과 관찰 → 다음 행동이 하나의 흐름으로 이어지고,
**상태 관리**와 **종료 조건**을 둔다.

---

## 루프

```
목표 입력 ("이번 주 운영 브리핑 카드뉴스")
   │
   ▼
┌─ 계획 ─────────────────────────────────────┐
│ 지금 단계에서 무엇을 알아야 하는가?         │
│ 어떤 도구를 어떤 순서로 부를 것인가?        │
└──────────────┬─────────────────────────────┘
               ▼
        ┌─ 도구 호출 ─┐
        │ TOOLS.md    │──── 실패 ──▶ 재시도 / 대체 경로 / 중단
        └──────┬──────┘
               ▼
┌─ 결과 관찰 ────────────────────────────────┐
│ 스키마에 맞는가? 빈 결과인가?               │
│ 근거로 쓸 수 있는가, 추측인가?              │
└──────────────┬─────────────────────────────┘
               ▼
┌─ 다음 행동 결정 ───────────────────────────┐
│ (a) 도구를 더 부른다        → 계획으로     │
│ (b) 사람에게 묻는다         → waiting_for_user │
│ (c) 결과를 만든다           → 다음 단계로  │
│ (d) 종료 조건에 걸렸다      → stopped      │
└────────────────────────────────────────────┘
```

## 상태

| 상태 | 뜻 | 완료인가 |
|---|---|---|
| `planning` | 다음 행동을 정하는 중 | 아니다 |
| `running` | 도구 호출 중 | 아니다 |
| `waiting_for_user` | 사람의 선택·승인 대기 — **정상 상태** | 아니다 |
| `failed` | 재시도 소진. 부분 결과는 보존 | 아니다 |
| `stopped` | 종료 조건에 걸림 | 아니다 |
| `done` | 사람이 최종 승인함 | 그렇다 |

에이전트가 "끝냈다"고 말하는 것과 결과물이 실제로 만들어진 것은 다르다.
`done`은 **사람이 최종 승인**하고 **파일이 실제로 열리는 것을 확인**했을 때만 붙인다.

## 저장하는 것

세션 ID만 저장한다고 화면 상태와 작업이 복구되지는 않는다.

| 항목 | 왜 필요한가 |
|---|---|
| `run_id` · `fixture_id` 또는 실제 실행 표시 | 어느 브리핑인지, 픽스처인지 실제인지 |
| 엔진 · 세션 ID | 앞선 대화를 이어가기 위해 |
| 현재 단계 · 상태 | 새로고침·재시작 후 복원 |
| 대기 질문 · 선택지 · `question_id` · `version` | 지난 질문에 뒤늦게 온 답을 거르기 위해 |
| 사용자 답변 | 같은 질문을 다시 묻지 않기 위해 |
| 도구 호출 기록 (입력·출력·소요·성공 여부) | 실행 로그 화면과 평가 |
| 대화 전문 + 요약 인덱스 | 요약만 남기면 사후에 무엇이 오갔는지 되짚을 수 없다 |
| 누적 토큰·비용·경과 시간 + `usage_known` | 종료 조건 판정과 병목 분석. **모르는 것과 0을 구별한다** |
| 승인 기록 (도구 · 대상 · 토큰 · 시각) | 쓰기 게이트 판정과 `revert_issue`의 대상 범위 |
| 지금까지의 스토리보드 · 카드 · 차트 | 부분 결과 보존 |

## 종료 조건

무한 루프와 비용 폭주를 막는다. 하나라도 걸리면 `stopped`으로 남기고 사람에게 넘긴다.

| 조건 | 기본값 | 누가 검사 | 걸렸을 때 |
|---|---|---|---|
| 최대 루프 반복 | 20 | **SDK** `maxTurns` | 지금까지의 결과를 보여주고 계속할지 묻는다 |
| 누적 비용 | $1.00 | **SDK** `maxBudgetUsd` | 위와 같음. 결과가 `subtype: error_max_budget_usd`로 온다 |
| 최대 도구 호출 | 40 | 내 코드 | 위와 같음 |
| 누적 입력 토큰 | 200,000 | 내 코드 | 위와 같음 |
| 실행 시간 | 10분 | 내 코드 | 위와 같음 |
| 같은 도구 연속 호출 | 3회 | 내 코드 | 진전이 없다고 보고 중단 |

`waiting_for_user`로 보낸 시간은 실행 시간에 넣지 않는다. 사람을 기다리는 건 정상이다.

종료는 **코드가 검사**한다. 모델의 "다 했다" 한 마디에 맡기지 않는다.
경계는 초과(>)다 — 상한값 자체는 허용하고 그 다음부터 막는다.

`error_max_budget_usd`로 끝난 결과는 `usage`가 예산을 넘긴 마지막 응답을 빼고 보고한다.
그 경우 비용은 `total_cost_usd`·`modelUsage`에서 읽고, 읽을 수 없으면 `usage_known: false`로 남긴다.

## 새로고침과 서버 재시작

| 상황 | 서버 작업 | 앱이 해야 할 일 |
|---|---|---|
| 브라우저 새로고침 | 살아 있을 수 있다 | 저장된 상태를 다시 그린다 |
| 서버 재시작 | 메모리에서 기다리던 콜백은 **사라진다** | 중단 상태를 보여주고 저장된 맥락으로 재개하거나 재시도하게 한다 |

## 승인 게이트가 루프 안에서 어디에 있는가

쓰기 도구는 「다음 행동 결정」의 (a)로 갈 수 없다. 반드시 (b) `waiting_for_user`를 지난다.

```
에이전트: "이 신호는 이슈로 남길 만하다" (제안)
   │
   ▼  ── PreToolUse 훅: 도구 표면·토큰 출처 2종만 검사 ──┐
   │      (승인 기록은 검사하지 않는다 — 훅이 canUseTool   │
   │       보다 먼저 돌아 그 시점에 승인이 없기 때문)      │
   ▼  ── _meta requiresUserInteraction → 항상 콜백 ───┤
   │                                                  ▼
waiting_for_user  ──▶ 사람이 내용을 보고 승인      게이트 3겹
   │                        │                      (DECISIONS.md D14)
   │                        ▼                         ▲
   │                 앱이 1회용 approval_token 발급    │
   ▼                        │                         │
running  ◀──────────────────┘                         │
   │                                                  │
   ▼  ── MCP 서버: 토큰 검사 → 무효면 거절 ───────────┘
   │
   ▼
실행 → 승인 기록에 저장 (revert_issue 의 대상 범위가 된다)
```

토큰은 한 번 쓰면 소멸한다. 같은 승인을 두 번 제출해도 이슈는 한 번만 만들어진다.

## 역할 분리

**이번 범위에서는 나누지 않았다.** 단일 루프로 구현한다. 설계만 남겨 둔다.

| 역할 | 하는 일 | 넘기는 값 |
|---|---|---|
| 조사 | 네 축 수집, 신호 후보 추림 | 신호 목록 + 근거 출처(도구·필드) |
| 작성 | 스토리보드와 원고 | 카드별 문장 + 근거 출처 |
| 검토 | 환각·누락 점검 | 지적 사항 |

나누지 않은 이유는 이번 확장 두 개(독립 MCP 서버·되돌리기)에 범위를 썼기 때문이다.
검토 에이전트를 붙이면 환각률이 내려가는지가 다음 실험 후보다. `README.md`의 「남은 과제」 참고.
