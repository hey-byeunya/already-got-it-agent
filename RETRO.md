# 회고 — 1차 (2026-09-10)

Main Quest 4 「이미 있어」 주간 운영 브리핑 에이전트. 인상이 아니라 커밋 기록으로 적는다.

## 숫자

| | |
|---|---|
| 전체 커밋 46건 중 **오늘 37건** | 하루에 프로젝트의 80% |
| 화면 이식(`5f5c9aa`, 02:20) 이후 **29건 중 27건이 `app/` 만** | 기능이 아니라 화면 다듬기 |
| 그중 **11건은 `.tsx`/`.css` 만** | 색·라벨·문장부호·쪽 넘김·접기 |
| `app/app/runs/[id]/page.tsx` **1,220줄** | 한 줄 고치려고 1,220줄을 열다섯 번 읽었다 |
| `DECISIONS.md` 33항목 중 **9개(D25~D33)가 오늘** | 대부분 「결함을 찾았다」 기록 |

토큰의 가장 큰 몫은 **한 파일을 열다섯 번 다시 읽은 것**이다.
「스크린샷 하나 → 지적 하나 → 파일 전체 읽기 → 고치기 → 빌드 → 캡처 → 커밋 → 푸시」가 열다섯 번 돌았다.

## 누수 네 갈래

### ① 화면 규약이 PRD에 없었다 — 커밋 11건

PRD 6절은 「무엇을 보여줄지」를 체크박스로 적었지만 「어떻게 읽힐지」는 한 줄도 없었다.
그래서 아래가 전부 리뷰 때 하나씩 발견됐다.

> 버튼 이름은 한글인가 영어인가 · `LIVE` 는 초록인가 호박인가 · 대시는 `—` 인가 `-` 인가 ·
> 목록은 몇 건씩인가 · 시각은 UTC 인가 현지인가 · 긴 목록은 펼치는가 접는가 · 설명은 본문인가 툴팁인가

일곱 줄이면 적었을 규약이 커밋 열한 건이 됐다.
→ **PRD 7절 「표시 규약」** 으로 옮겨 적었다.

### ② 「모르는 것을 0으로 적지 않는다」를 데이터에만 적용했다

이 프로젝트의 핵심 규율인데 **상태**에는 적용하지 않았다. 같은 모양의 결함이 넷이다.

| 커밋 | 증상 | 공통점 |
|---|---|---|
| `137b663` | 모드 배지가 `.env.local` 변경을 못 따라가 `LIVE` 라고 거짓말 | |
| `b72a888` | opencode 경로가 `OPS_MODE:'fixture'` 를 박아 두어 모드를 무시 | **화면이 코드가** |
| `03e9291` | 로그 시각이 UTC 라 같은 화면의 `created` 와 9시간 어긋남 | **보장하지 않는 것을** |
| `5899ab6` | 질문 묶음에서 하나만 눌러도 게이트가 닫혀, 안 고른 답이 고른 것으로 기록 | **단언했다** |

→ **PRD 8절 「관측 계약」** — 화면 값마다 *출처 / 없을 때 뜻 / 거짓일 수 있는 조건*.

### ③ 근본 원인 대신 증상에 처방했다 — 가장 비쌌던 실수

opencode 가 20분간 0줄로 멈추는 문제에서,
`.env.local` 변수를 빼 가며 **재현 실행 다섯 번**을 돌리고 결론은 「원인 모름」이었다.
그러고 90초 감시(`57bfcbb`)와 안내 문구를 붙였다.

진짜 원인은 `5d1294e` 에 있다 — `execFile` 이 **stdin 을 열린 파이프로 남겨** 부트스트랩이 거기서 잤다.
`spawn` + `stdio: ['ignore','pipe','pipe']` 로 끝났다.

**stderr 를 한 번도 읽지 않았다.** 같은 커밋이 stderr 수집을 함께 넣은 것이 그 증거다.
외부 프로세스를 붙이면서 stdin·stderr·종료코드 셋 중 **둘을 안 보고** 환경변수를 이분 탐색했다.

→ **PRD 9절 「외부 프로세스 계약」** — 엔진마다 다섯 항목을 붙이기 전에 적는다.

### ④ 자동 편집이 조용히 어긋났다

| 무엇 | 어떻게 드러났나 |
|---|---|
| em-dash 치환 1차가 `types.ts` 주석 6줄을 잘못 고침 | diff 를 눈으로 봐서 |
| `text-indent` 를 두 번 틀림 (덩이 전체 → 문단까지 들여씀) | 화면에 그려 보고 |
| 파비콘 XML 주석에 `--` 를 넣어 **파일 전체가 깨짐** | **콘솔에 아무 말도 없었다.** 크기별로 그려 보다가 |
| 브라우저 오조작으로 데모 실행 2건 삭제 | 목록 수가 줄어서 |
| (이전 세션) 파이썬 편집 스크립트가 중간 실패 후 조용히 롤백 | 며칠 뒤 DOM 을 보다가 |

공통점: **적용 결과를 눈으로 확인하기 전까지는 성공한 줄 알았다.**

## 잘 된 것 — 다음에도 가져간다

- `DECISIONS.md` 33항목이 **전부 관측한 문제에 붙어 있다.** 「왜 이렇게 했나」를 나중에 재구성하지 않아도 된다
- **픽스처 모드**가 평가·캡처를 재현 가능하게 만들었다. 모델을 안 부르고도 화면 네 상태를 그릴 수 있다
- **「확인 못 했다」를 그대로 말한 것.** 질문 게이트를 고친 뒤 「진짜 게이트에서는 확인 못 했다」고 적었더니
  브리핑을 한 번 더 돌리는 결정으로 이어졌다. 숨겼으면 그 버그는 그대로 남았다

## 다음 퀘스트의 작업 규칙

1. **PRD 7·8·9절을 먼저 채우고 시작한다.** 비어 있으면 화면을 만들지 않는다
2. **화면 리뷰는 한 바퀴 몰아서.** 스크린샷 하나 → 커밋 하나가 아니라, 전부 모아 한 번에.
   왕복 15번이 3번이 된다
3. **외부 프로세스는 stderr 부터.** 재현·이분 탐색 전에 stderr·종료코드·stdio 설정을 읽는다
4. **자동 편집은 적용 전에 diff 를 낸다.** 그리고 전부 성공했을 때만 쓴다 (부분 적용 금지)
5. **화면 파일은 300줄을 넘기지 않는다.** 1,220줄이면 한 줄 고치는 데 1,220줄을 읽는다

## 갚은 빚 / 남은 빚

- [x] PRD 7·8·9절 추가
- [x] 이 회고 기록
- [ ] `runs/[id]/page.tsx` 1,220줄 → 컴포넌트 분리
- [ ] 여러 질문 묶음에서 게이트 동작 실측 (에이전트가 질문을 둘 이상 낼 때)
