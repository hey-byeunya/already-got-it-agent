# DECISIONS — 선택과 이유

이 문서는 **나에게** 물어 정한 것을 기록한다. 완성된 앱이 **그 앱 사용자에게** 묻는 질문
(기간·축·트렌드 선택 등)은 여기가 아니라 실행 상태 저장소에 남는다.

---

## D1. 도메인

- **결정**: 「이미 있어」(already-got-it)를 **운영하는 개발자 본인**을 위한 주간 운영 브리핑 카드뉴스
- **이유**: 직접 만들고 배포·운영 중인 앱이라 데이터 모델과 실패 모드를 안다. 반복 업무가
  "매주 운영 상태를 점검하는 일"로 또렷하고, 시스템·사용자·개발·트렌드 네 축이 도구 경계와
  자연스럽게 일치한다.
- **처음 방향에서 바꾼 것**: 초안은 서비스 **사용자**를 위한 계절·날씨 맞춤 재고 점검이었다.
  운영자 관점으로 옮기면서 데이터 출처와 시각물이 함께 바뀌었다(D11 참고).
- **결정일**: 2026-09-09

## D2. 실행 엔진 → **Claude Agent SDK**

- **결정**: `@anthropic-ai/claude-agent-sdk` (TypeScript)
- **선택지**: Claude Agent SDK / Claude CLI / OpenCode CLI

| | 앱에서 연결하는 방식 | 앞선 대화를 이어가는 방식 |
|---|---|---|
| **Claude Agent SDK** | 코드에서 `query({ prompt, options })` 호출 | 저장한 세션 ID를 `options.resume`에 전달 |
| Claude CLI | 서버에서 `claude -p` 실행, JSON 결과를 읽음 | `--resume`에 그 프로젝트의 세션 ID |
| OpenCode CLI | 서버에서 `opencode run --format json`, 줄 단위 이벤트 | `--session`에 그 프로젝트의 세션 ID |

- **이유**: 이 앱이 필요한 네 가지가 SDK에서 바로 온다 — ① `AskUserQuestion`을 `canUseTool`
  콜백에서 가로채 질문 대기를 만들고 ② 같은 콜백에서 쓰기 도구 승인을 받고 ③ `options.resume`으로
  이어가고 ④ `maxTurns`·`maxBudgetUsd`로 상한을 건다. CLI 두 개는 `need_input` 규약과 토큰·반복·승인
  제어를 전부 직접 설계해야 한다.
- **초기 판단을 뒤집은 지점**: 처음에는 배포(Vercel 서버리스)를 전제로 일반 Anthropic SDK의
  Tool Runner를 골랐다. 서버리스에서 CLI를 돌릴 수 없고, 공개 URL에 Bash·파일 도구를 얹는 것이
  위험해서였다. **제출 요건에서 배포 URL이 빠지면서** 그 두 제약이 사라져 수업이 지정한 세 선택지
  중 하나를 그대로 쓸 수 있게 됐다.
- **확인한 옵션**: `resume` · `maxTurns` · `maxBudgetUsd` · `allowedTools` · `disallowedTools` ·
  `permissionMode` · `mcpServers` · `model` · `env`
- **차단할 내장 도구**: `Bash` · `Write` · `Edit` · `Read` · `Glob` · `Grep` 등. `disallowedTools`에
  **맨이름으로** 적으면 도구 정의가 요청에서 빠져 **모델이 존재조차 모른다.** 이 앱의 도구는 도메인
  API 6개와 차트 하나뿐이라 파일·셸 표면이 필요 없고, 열어 두면 엉뚱한 경로로 샐 여지만 남는다.
- **결정일**: 2026-09-09

## D14. ⚠️ 승인 게이트를 3중으로 둔다

- **결정**: 쓰기 도구 승인을 **세 겹**으로 막는다
- **왜 한 겹으로는 안 되는가**: Agent SDK 문서가 명시한다 —

  > **"Auto-approved tools never reach `canUseTool`."** 앞선 단계에서 승인된 호출은
  > (allow 규칙 포함) `canUseTool` 콜백을 건너뛰므로, 거기 둔 권한 검사가 그 도구에 대해서는
  > **조용히 우회된다.**

  즉 도구 9개를 모두 `allowedTools`에 넣으면 `create_github_issue`가 자동 승인되고 **승인 화면이
  아예 뜨지 않는다.** 게이트가 있는 줄 알았는데 없는 상태 — 이 서비스가 겨냥하는 "조용한 실패"를
  서비스 자신이 저지르는 셈이다.

| 겹 | 무엇 | 왜 이 겹이 필요한가 |
|---|---|---|
| ① MCP 서버의 `_meta` | 쓰기 도구에 `_meta["anthropic/requiresUserInteraction"]` 선언 | 이게 붙으면 **allow 규칙이 매치돼도 항상 콜백으로 떨어진다.** 게이트가 앱 설정이 아니라 **도구의 성질**이 된다 (Claude Code v2.1.199+) |
| ② MCP 서버의 `approval_token` 검사 | 유효한 1회용 토큰 없이는 서버가 거절 | 앱을 우회해 서버에 직접 붙어도 막힌다 |
| ③ `PreToolUse` 훅 | **설정과 무관한 불변식 두 개**를 검사 (바로 아래) | 훅은 **모든 단계보다 먼저** 실행되고, 훅의 deny는 `bypassPermissions`에서도 유효하다 |

- **추가 규칙**
  - **쓰기 도구를 `allowedTools`에 넣지 않는다.** 읽기 도구 5개만 자동 승인한다.
  - **`permissionMode`는 `default`.** 문서가 잠금 조합으로 권하는 `allowedTools` + `dontAsk`는
    이 앱에 **쓸 수 없다** — `dontAsk`는 `canUseTool`을 절대 호출하지 않아 `AskUserQuestion`(질문
    대기)과 쓰기 도구 승인이 **거부**된다.

**③ 훅이 검사하는 것 — 설정이 어떻든 참이어야 하는 것만**

| 규칙 | 무엇을 막는가 | 왜 훅이어야 하는가 |
|---|---|---|
| `tool_not_allowed` | 도메인 도구 9개와 `AskUserQuestion` 밖의 도구 | `disallowedTools`가 잘못 설정되거나 `bypassPermissions`가 켜져도 남는 검사 |
| `model_supplied_approval_token` | 모델이 보낸 입력에 `approval_token`이 들어 있는 경우 | ①②는 "**유효한** 토큰이면 실행한다"까지만 보장한다. 토큰이 **어디서 왔는지**는 보장하지 않는다 |

두 번째 규칙을 왜 두는가 — 지금은 모델이 토큰을 알 경로가 없다. 하지만 나중에 입력을 되돌려 주는
도구가 생기거나 실행 로그를 모델에 먹이는 화면이 생기면 그 경로가 열린다. 불변식을 우연에 맡기지
않고 못 박는다. 토큰은 `canUseTool`이 사람의 승인을 받은 뒤 `updatedInput`으로만 주입한다.

**훅이 하지 않는 것 — "승인 기록이 있는지" 검사.** 훅은 `canUseTool`**보다 먼저** 돌기 때문에
그 시점에는 아직 승인이 없다. 거기서 막으면 **사람이 묻기도 전에 모든 쓰기가 차단된다.**
처음 설계(이 문서의 앞 판본)에는 이 검사가 들어 있었고, 구현하면서 순서를 짚어 보다 발견해 뺐다.
검사(`agent/test/hook.test.ts`)에 경계로 남겼다 — "토큰이 없는 쓰기 호출은 통과시킨다".

- **자체 검사**: `allowedTools`의 맨이름 항목이 콜백을 가리면 SDK가 `CLAUDE_SDK_CAN_USE_TOOL_SHADOWED`
  코드로 Node 경고를 한 번 띄운다. `process.on('warning', ...)`로 잡아 **경고가 뜨면 실패로 처리**한다.
  게이트가 가려진 상태로 넘어가는 것을 코드가 막는다.
- **결정일**: 2026-09-09

## D13. 도메인 도구는 독립 MCP 서버로 한 번만 구현한다

- **결정**: `mcp-server/`에 stdio MCP 서버를 만들고, 앱은 `mcpServers` 옵션으로 붙여 쓴다

```
mcp-server/            ← 도구 9개. 구현은 여기 한 곳
   ↑ 붙여 씀
app/  (Next.js)        ← Agent SDK 가 외부 MCP 서버로 연결
Claude Code            ← .mcp.json 에 등록하면 같은 서버를 그대로 사용
```

- **이유**: 인프로세스 등록은 Agent SDK가 커스텀 도구를 받는 방식이라 "딸려오는 것"에 그친다.
  독립 서버로 만들면 "내 도메인 시스템을 에이전트의 도구로 노출"이 실제로 성립하고, 도구를 두 번
  구현하지 않으니 증분도 작다. 부수 효과로 같은 서버를 Claude Code에서도 쓸 수 있다.
- **얻은 것 하나 더**: 승인 게이트를 **앱 화면이 아니라 서버 경계**에 둘 수 있다(D14 ①②).
  앱 안의 약속이 아니라 경계에서 강제되는 규칙이 된다.
- **결정일**: 2026-09-09

## D12. 언어·스택 → Next.js + TypeScript

- **결정**: Next.js (App Router) + TypeScript. 로컬 `npm run dev`로 실행
- **이유**: 실행 로그·비용·결과물을 **화면으로** 보여줘야 하므로 웹 UI가 필요하다.
  `already-got-it`과 같은 스택이라 Supabase 클라이언트 패턴과 환경변수 규율을 그대로 재사용한다.
- **결정일**: 2026-09-09

## D9. 상태 저장과 재개

- **결정**: 실행은 백그라운드 작업으로 돌리고 **매 단계마다 상태를 저장**한다. 브라우저는 그 상태를 읽어 그린다
- **이유**: 배포를 안 하므로 서버리스 실행 시간 제약은 없지만, "중간에 끊겨도 이어서 진행"은 여전히
  요구된다. 세션 ID만 저장한다고 화면 상태와 작업이 복구되지는 않는다.
- **구별해서 구현할 것**

| 상황 | 서버 작업 | 앱이 해야 할 일 |
|---|---|---|
| 브라우저 새로고침 | 살아 있을 수 있다 | 저장된 상태를 다시 그린다 |
| 서버 재시작 | 메모리에서 기다리던 콜백은 **사라진다** | 중단 상태를 보여준 뒤 저장된 맥락으로 재개하거나 재시도하게 한다 |

- **결정일**: 2026-09-09

## D10. 관리자 집계는 `security definer` 집계 전용 RPC로

- **결정**: 집계값만 돌려주는 `security definer` Postgres 함수를 새로 만든다. 원시 행과 개인정보는 반환하지 않는다
- **이유**: 운영 브리핑은 전체 사용자 지표를 봐야 하는데 anon 키 + RLS로는 막힌다. 그렇다고
  `service_role` 키를 쓰면 기존 원칙("어디에도 쓰지 않는다")이 깨지고, 키 하나가 전체 데이터
  접근권이 된다. 집계 전용 함수는 **필요한 것만 통과시키는 창**이라 권한 최소화에 맞는다.
- **강제 방법**: 함수 시그니처가 아예 집계 타입만 반환하도록 만든다. 원시 행을 반환할 수 없는 구조다
- **결정일**: 2026-09-09

## D11. 카드 시각물 → SVG 차트 + 로컬에서 만든 표지 삽화

- **결정**: 지표 카드는 `render_chart`가 SVG로 렌더링. 표지 삽화만 로컬에서 Antigravity CLI(`agy`)로
  미리 만들어 저장소에 커밋
- **이유**: 도메인이 재고 점검(물건)에서 운영 지표(숫자)로 바뀌면서 어울리는 시각물도 바뀌었다.
  **생성 삽화는 배포 성공률이나 가입 추이를 그릴 수 없다.** 카드 대부분이 수치를 보여줘야 하므로
  차트가 맞고, 삽화가 어울리는 건 표지 한 장뿐이다.
- **부수 효과**: 런타임 이미지 생성이 없어져 이미지 API·키·비용이 모두 불필요해졌다
- **유지하는 원칙**: `opened_ok`("요청 성공 ≠ 결과물 생성"). 차트는 **근거 데이터가 있고 SVG가 실제로
  그려졌을 때만** 완료로 표시한다
- **초안에서 바꾼 것**: 초안의 `generate_image`(매 실행마다 `agy` 호출)를 `render_chart`로 교체
- **결정일**: 2026-09-09

## D7. 종료 조건 기본값

| 조건 | 기본값 | 누가 검사 |
|---|---|---|
| 최대 루프 반복 | 20 | **SDK** `maxTurns` |
| **누적 비용** | **$1.00** | **SDK** `maxBudgetUsd` → 결과 `subtype: error_max_budget_usd` |
| 최대 도구 호출 | 40 | 내 코드 |
| 누적 입력 토큰 | 200,000 | 내 코드 |
| 실행 시간 | 10분 (`waiting_for_user` 제외) | 내 코드 |
| 같은 도구 연속 호출 | 3회 | 내 코드 |

- 하나라도 걸리면 `stopped`으로 남기고 사람에게 넘긴다. 종료는 **코드가 검사**하고, 모델의
  "다 했다" 한 마디에 맡기지 않는다.
- 경계는 초과(>)다. 상한값 자체는 허용하고 그 다음부터 막는다 — "같은 도구 3회"면
  3번째까지는 일하고 4번째에서 중단한다 (`agent/src/limits.ts` 주석과 짝이다).
- `waiting_for_user`로 보낸 시간은 실행 시간에 넣지 않는다. 사람을 기다리는 건 정상이다.
- **비용 한도는 직접 만들지 않았다.** SDK의 `maxBudgetUsd`가 이미 한다.

> ⚠️ **`total_cost_usd`는 클라이언트 측 추정값이다.** SDK가 번들된 단가표로 로컬 계산하며, 단가
> 변경·모델 미인식·모델링 불가한 과금 규칙에서 실제 청구와 어긋날 수 있다. 문서가 "이 값으로
> 사용자에게 청구하거나 재무 결정을 내리지 말라"고 명시한다. 종료 조건과 화면 표시에는 쓰되,
> `README.md`의 「밝혀 두는 것」에 추정값임을 적는다.

- **결정일**: 2026-09-09

## D15. 토큰·비용 집계는 결측을 0으로 뭉개지 않는다

- **결정**: `usage_known` 플래그를 두고, 사용량을 **모르는 것과 0인 것을 구별**한다
- **왜 필요한가**: Agent SDK 문서가 비용 필드를 신뢰할 수 없는 경우를 직접 나열한다 —
  세션 크래시(`error_during_execution`)에서는 모든 비용 필드가 0일 수 있고, 예산 초과
  (`error_max_budget_usd`)에서는 `usage`가 마지막 응답을 빼고 보고한다. 0으로 받아 적으면
  **비용이 안 들었다고 잘못 보고**하게 된다.
- **함께 지킬 집계 규칙 3개**
  1. 병렬 도구 호출은 assistant 메시지 `id`가 같다 → **ID로 중복 제거**
  2. per-step `output_tokens`는 **placeholder**다 → 출력 토큰은 **result 메시지**에서 읽는다
  3. `usage`는 서브에이전트를 제외한다 → 전체는 `total_cost_usd`·`modelUsage`로 집계
- **출처**: Day 38 하네스 `harness/loop.py:114 _account()`의 같은 아이디어를 가져왔다
- **결정일**: 2026-09-09

## D18. 자격증명 — env 키가 없다고 막지 않는다

- **결정**: `ANTHROPIC_API_KEY` 가 없어도 실행을 막지 않는다. 어느 출처로 갈지만 알린다.
- **왜**: SDK 는 `ANTHROPIC_API_KEY` → `ANTHROPIC_AUTH_TOKEN` → **저장된 로그인(구독)** 순으로
  자격증명을 찾는다. **환경변수 키가 없다고 자격증명이 없다는 뜻이 아니다.**
  처음 구현에서는 키가 없으면 `exit(2)` 했는데, 그러면 구독으로 도는 경로를 코드가 닫아 버린다.
- **확인함**: `.env.local` 의 API 키를 주석 처리하고 돌려 `done (success)` 를 받았다
  (`runs/sub-test`). 저장된 로그인으로 흘렀다.
- **비용 표시가 달라진다**: 화면과 CLI 가 자격증명 출처를 함께 적는다.

| 출처 | 비용 표시 |
|---|---|
| `ANTHROPIC_API_KEY` | 추정 금액 + "API 사용량 크레딧에서 차감" |
| 저장된 로그인(구독) | 추정 금액 + "**API 크레딧에서 차감되지 않는다**" |

  같은 숫자를 두 지갑에 같은 뜻으로 적으면 안 된다. 구독 실행에서 `$0.0445` 를 청구액처럼
  보여주면 D15 가 막으려는 것과 같은 종류의 잘못된 보고가 된다.

- **결정일**: 2026-09-09

## D4. (폐기) 로그인 없는 공개 데모

- **폐기 이유**: 제출 요건에서 배포 URL이 빠져 "링크만으로 접속" 요구가 사라졌다
- **대신 남긴 것**: **픽스처 실행 모드**. 평가 재현성과 캡처 재현성을 위해서이고, 실제 API 호출을
  아끼는 효과도 있다
- **결정일**: 2026-09-09

## D16. 확장 시도 두 가지

- **결정**: ① 독립 MCP 서버로 도메인 도구 노출(D13) ② 되돌리기 — 승인해 만든 이슈 취소
- **되돌리기를 고른 이유**: 쓰기 도구의 위험을 되돌릴 수 있게 만드는 일이라 권한 최소화와 이어진다.
  `revert_issue`는 **임의 이슈를 닫지 못한다** — 이 실행의 승인 기록에 있는 이슈 번호만 대상이다.
- **미룬 확장**: 멀티 에이전트, 자동 트리거(스케줄·웹훅), n8n 오류 감지 → `README.md`의 「남은 과제」
- **결정일**: 2026-09-09

## D17. 미정이었던 것 — 확인 결과

| 항목 | 확인 결과 (2026-09-09) |
|---|---|
| Vercel API 조회 범위 | **배포 이력·상태·커밋 SHA 는 읽힌다** (`/v6/deployments`, 180일 29건 확인). 빌드 오류 원문은 `/v13/deployments/{id}` 로 실패 건만. **함수 오류 수는 조회 경로가 없다** — 관측 API 는 404, 런타임 로그는 배포별·단기 보관이라 주간 집계로 못 쓴다 → live 모드에서 `unavailable_fields: ["function_errors"]` 로 내린다 |
| 카드 비율 | **1080×1350** (4:5) 로 확정 |
| 집계 RPC 지표 목록 | **확정** — `signups`(auth.users) · `owned_created` · `wish_created` · `active_users`(있템/위시를 만들거나 고친 distinct 사용자). 정의를 `supabase/ops_metrics.sql` 주석에 박아 두었다 |
| 화면 전환율·유입 | 여전히 불가 — 기반 앱에 계측이 없다 (`README.md` 남은 과제) |

## D19. live `web_search` → 범용 검색 대신 **의존성 릴리스·보안 권고**로 좁힘

- **결정**: live 모드의 `web_search` 는 npm registry 와 GitHub Advisory Database 두 곳만 본다.
- **왜**: 도구 description 이 이미 범위를 정해 놓았다 — "이 앱이 실제로 쓰는 의존성의 릴리스 노트나
  보안 권고를 찾을 때만 호출한다". 범용 검색 API 키를 새로 발급받는 대신 그 문장을 그대로 구현했다.
  **설명과 구현이 어긋나지 않는 쪽**을 골랐다.
- **얻은 것**: 결과가 결정적이고, `published_at` 이 실제 값이며, 앱이 쓰지 않는 패키지 소식이
  섞여 들어오지 않는다. 새 자격증명도 필요 없다 (npm 은 키 없음, 권고는 기존 `GITHUB_TOKEN`).
- **못 하는 것**: 의존성과 무관한 일반 IT 트렌드. `README.md` 「밝혀 두는 것」에 적었다.
- **첫 실행에서 실제로 잡은 것**: `next` 의 critical 권고(GHSA-2xp9-vwfh-vxw4 — AVIF 이미지
  최적화 경로의 인증 없는 RCE). 기반 앱이 next 16.2.11 이므로 실제로 해당한다.
- **결정일**: 2026-09-09

## D20. 집계 RPC 에 **토큰 인자**를 둔다 — anon 키는 공개 값이다

- **결정**: `public.ops_user_metrics(p_token, p_since, p_until, p_granularity)` 가 첫 인자로
  비밀값을 받고, PostgREST 가 노출하지 않는 `private.ops_config` 의 값과 대조한다.
- **왜**: D10 이 `service_role` 대신 `security definer` 집계 함수를 쓰기로 했는데, 그 함수를
  anon 에게 열어 주는 순간 **anon 키를 가진 누구나 전체 가입자 수를 읽을 수 있다.**
  anon 키는 Next.js 앱에서 브라우저로 내려가는 공개 값이라 비밀이 아니다.
  D10 만으로는 개인정보는 막히지만 전사 지표는 열려 있었다.
- **어떻게**: 스키마 `private` 는 PostgREST 가 노출하지 않으므로 REST 로 그 테이블을 읽을 수 없다.
  에이전트는 `.env.local` 의 `OPS_METRICS_TOKEN` 으로 값을 넘긴다.
- **여전히 지킨 것**: 반환형이 `jsonb` 집계값이다. 행 집합을 반환하지 않으므로
  **원시 행 반환이 시그니처 차원에서 불가능하다.**
- **한계**: 토큰 대조는 상수 시간 비교가 아니다. 이 규모에서는 Supabase 쪽 호출 제한에 맡긴다.
- **결정일**: 2026-09-09

## D21. live 모드의 실제 쓰기는 **승인과 별개의 스위치**로 한 번 더 막는다

- **결정**: `OPS_ALLOW_LIVE_WRITES=1` 이 아니면 live 모드에서도 `create_github_issue` ·
  `revert_issue` 가 `live_writes_disabled` 로 거절된다. 기본값은 꺼짐.
- **왜**: 승인 게이트 ①②③ 은 "이 이슈 내용이 맞는가"를 묻는다. 그것과
  "실제 공개 저장소를 바꿔도 되는가"는 **다른 질문**이다.
  live 로 읽기만 돌려 보는 동안 사람이 무심코 승인하면 진짜 이슈가 생긴다.
  두 질문을 한 스위치에 묶으면, 실수의 값이 되돌릴 수 없는 쪽으로 커진다.
- **어디에 두었나**: 서버의 도구 핸들러 안, `consume()`(토큰 검사) **뒤**다.
  앞에 두면 스위치가 꺼져 있을 때 승인 토큰이 소모되지 않아, 토큰 재사용 검사와 어긋난다.
- **검증**: `npm run smoke-live` 9번 — 유효한 승인 토큰으로 호출해도 거절되는 것을 확인한다.
- **결정일**: 2026-09-09

## D22. 카드 PNG → **로컬 헤드리스 브라우저**로 굽는다

- **결정**: `export_cardnews` 가 `--headless --screenshot` 으로 카드 SVG 를 PNG 로 바꾼다.
  경로는 `OPS_CHROME_PATH` 로 바꿀 수 있고, 없으면 `rasterizer_not_found` 로 알린다.
- **왜 이 방법인가**: 이 기계에 `rsvg-convert`·`resvg`·ImageMagick 이 전부 없었다.
  네이티브 바이너리를 딸린 npm 패키지(sharp·resvg-js)를 넣는 대신, 이미 설치돼 있고
  **한글 폰트를 시스템에서 그대로 가져오는** 브라우저를 썼다. 런타임 이미지 생성 API 를
  부르지 않는다는 D11 의 원칙은 그대로다 — 내 SVG 를 내 기계가 굽는다.
- **`opened_ok` 를 어떻게 확인하나**: 변환 명령의 종료 코드를 믿지 않는다.
  PNG 시그니처와 IHDR 을 직접 읽어 1080×1350 을 대조하고, 바이트 수가 너무 작으면
  (글자가 하나도 안 그려진 경우) 실패로 본다. 한 장이라도 실패하면 ZIP 을 만들지 않는다.
- **ZIP 을 직접 쓴 이유**: 만드는 쪽과 확인하는 쪽을 분리하려고. 내가 쓴 ZIP 을
  **독립된 도구**(`unzip -l` · `unzip -t`)로 열어 검사한다. 같은 코드로 확인하면
  잘못 만든 ZIP 을 잘못 확인한다.
- **결정일**: 2026-09-09

## D23. 표지 삽화 — `agy` 우선, 로컬 폴백 (2026-09-09 갱신)

- **결정**: 표지 카드(`kind: "cover"`)의 삽화는 **`agy` 산출물이 있으면 먼저 쓰고,
  없으면 로컬 SVG 로 그린다.** 순서는 `OPS_COVER_ART` 로 바꾼다 (`auto`·`agy`·`local`,
  기본 `auto`). 어느 쪽인지는 `compose_card` 결과가 `cover_source`
  (`agy-asset`·`local-svg`)로 밝힌다.
- **왜 이렇게 나누는가**: 런타임에 `agy` 를 매번 부르면 같은 카드가 매번 다른 그림이 돼
  캡처 재현성이 깨지고, 실행마다 에이전트 호출 비용·대기가 붙는다 (D11).
  그래서 생성은 사람이 한 번 (`npm run generate-cover` → `mcp-server/assets/cover.svg`
  커밋), 사용은 매 실행이다. 재현성이 깨지지 않는다 — 같은 에셋 파일이면 같은 그림이다.
- **`agy` 실측 (2026-09-09)**: 앞 판본에는 "`agy` CLI 가 번들에 없다"고 적었다.
  다시 확인하니 `/opt/homebrew/bin/agy` (v1.1.28)가 PATH 에 있고 `agy -p` 가 동작한다.
  다만 이미지 전용 서브커맨드는 없고 에이전트 CLI라, 표지 생성은 `-p` 프롬프트로
  SVG 코드를 받아내는 스크립트로 감쌌다 (`mcp-server/scripts/generate-cover.mjs`,
  `OPS_AGY_BIN` 으로 경로 지정 가능). `agy` 가 없으면 스크립트는 만들지 않고
  로컬 폴백을 안내한다 — 없는 명령을 가정하지 않는다.
- **안전장치**: 에셋이 있는데 깨졌으면 로컬로 뭉개지 않고 `cover_not_svg` 로 거절한다.
  커밋된 파일이 깨진 것을 조용히 넘기면 "agy 삽화를 쓴다"는 보고가 거짓이 된다.
  `OPS_COVER_ART=agy` 인데 에셋이 없으면 `cover_asset_missing` 으로 거절한다.
  `<script>` 포함·1MB 초과 에셋은 저장도·삽입도 하지 않는다.
- **앞 판본에서 바꾼 것**: 로컬 SVG 를 기본값이 아니라 폴백으로 내렸다.
  로컬 격자(`coverArt`, `card_no` 종자)는 그대로 둔다 — 에셋이 없는 환경에서도
  표지가 만들어져야 하므로.
- **결정일**: 2026-09-09 (초판) · 2026-09-09 (agy 확인 후 agy-우선으로 갱신)

## D24. 첫 live 실행이 증명한 것 (2026-09-10)

- **실행**: `OPS_MODE=live node dist/src/run.js --run-id live-t1` (구독 로그인, 승인 없이).
  `done (success)` — 도구 호출 24회(MCP 기록 20건 — `AskUserQuestion`·스키마 거절은 서버 기록에 없다),
  실행 346.7초, 비용 $0.7940 추정(구독이라 청구되지 않음). 카드 6장 + PNG·ZIP·SOURCES.md 전부 열림.
- **읽기 4축**: Vercel(배포 0건)·GitHub(이슈·커밋·PR 0건)·web_search(릴리스·권고 12건 내외) 정상.
  Supabase는 `rpc_not_created` — 집계 함수 미생성이라 사용자 지표 축 전체가 `확인 못 함`이 됐다.
  **남은 사람 몫**: `supabase/ops_metrics.sql`을 SQL Editor에서 실행하고 `OPS_METRICS_TOKEN` 값을 맞춘다.
  → **해소 (2026-09-10)**: 실행 후 직접 호출로 확인. 7일 series + totals + 직전 기간 비교값,
  `unavailable_fields: []`, 집계값만(원시 행 없음). 이번 기간 전부 0 · 직전 기간 활성 1.
- **P1-7(a) live 증거**: 4번 카드가 지어낸 근거로 `source_not_found` 거절당하자 모델이
  `get_user_metrics`를 재호출하고, 그래도 안 되자 개발 활동 내용으로 카드를 바꿔 완성했다.
  거절이 작업 전체를 죽이지 않고 대체 경로로 이어진 첫 실제 사례다.
- **승인 게이트 live 증거**: 이슈 생성 제안 2건이 모두 거절됐고(플래그 없음),
  SDK `permission_denials` 2건이 기록에 남았다. `OPS_ALLOW_LIVE_WRITES=0`이라
  승인했어도 서버가 `live_writes_disabled`로 막는 이중 구조다.
- **D15 live 증거**: 신규 입력 토큰은 34에 불과했지만 캐시 읽기가 1,050,818이었다.
  캐시를 같은 무게로 셌다면 200,000 상한에 걸려 정상 실행이 중단됐을 것이다.
- **D19 live 증거**: `next` critical RCE 2건(GHSA-2xp9-vwfh-vxw4 포함, D19에서 미리 잡은 것과 동일),
  `react` XSS medium, `vitest` 경로 탐색 medium을 실제로 받아 카드화했다.
- **결정일**: 2026-09-10

## D25. 화면을 터미널 형태로 — 계기판에 가까운 값들에 자리를 준다

- **결정**: 화면 전체를 CLI 세션처럼 그린다. 신호등 점, `➜ brief run --fixture ...` 프롬프트 줄,
  `[ SECTION ]` 머리글, `████ 21%` ASCII 게이지, `--resume`·`--cards` 같은 플래그형 버튼,
  `[ TAIL -F ]` 로그 열. 다크 단일 테마이고 `prefers-color-scheme` 분기를 두지 않는다 —
  라이트 모드 기기에서도 같은 화면이 나와야 캡처가 재현된다.
- **왜**: 이 앱이 보여줘야 하는 것이 「예쁜 대시보드 카드」가 아니라
  **상한 대비 소진률 · `usage_known: false` · 폴링 정지 · 무효가 된 대기 콜백** 같은
  계기판 값이다. 둥근 카드와 배지로는 "모른다"와 "0"이 같아 보인다.
  터미널 형태는 그 둘을 다른 색·다른 문장으로 갈라 놓기 쉽다.
- **화면을 만들면서 알게 된 것 — 데이터는 이미 다 있었다.**
  `[ AXES ]` 4축 수치는 MCP 서버가 남기던 `toolcalls.jsonl` 에, 카드의 제목·본문·출처·심각도는
  `compose_card` 가 남기던 `cards/NN.json` 에, 로그 글리프는 `TraceEvent.kind` 에 이미 있었다.
  **화면이 그것을 안 읽고 있었을 뿐이다.** 새로 수집한 값은 하나도 없다 (`app/lib/derive.ts`).
- **새로 저장하기 시작한 것 셋**: 상한(`RunState.limits` — 전에는 트레이스 **문자열 안**에만 있어
  값으로 꺼낼 수 없었다) · 축(`focus`) · 기간(`period`).
- **화면을 열어 보고 고친 것 넷** (코드만 읽어서는 못 봤다):
  ① `[ PROGRESS ]` 의 `◆` 가 여러 개 동시에 켜져 어디서 멈춰 있는지 알 수 없었다
  ② 없는 실행(`ui-state.json` 이 없는 CLI 실행)을 열면 「불러오는 중」에서 영원히 멈췄다
  ③ 좁은 폭에서 비용이 행 밖으로 떨어져 어느 실행 것인지 읽히지 않았다
  ④ `iterations 0 / 20` — 도구를 14번 부른 실행에서 0 을 보여줬다. 아래 항목으로 이어진다.
- **관측할 수 없는 것은 게이지로 그리지 않는다.** 반복 횟수는 SDK 가 검사하는 값이고
  화면은 실제 턴 수를 알 수 없다. 그 자리를 **fresh input 토큰**(캐시 읽기를 뺀 값 — 상한이
  재는 바로 그 값)으로 바꾸고, 반복 상한은 "SDK 가 검사한다"고 문장으로 적었다.
  모르는 값을 그럴듯한 숫자로 채우지 않는다는 이 프로젝트의 규칙이 화면에도 그대로 적용된다.
- **바탕**: Claude Design 「운영 브리핑 개선안 v4 (터미널)」. 목업의 `[ STATE ]` 전환 버튼은
  화면을 갈아 끼우는 **데모 장치**라 이식하지 않았다 — 실제 실행에는 상태가 하나다.
- **결정일**: 2026-09-10

## D26. 바깥 참조에 링크를 붙이되, **픽스처 참조는 실제라고 말하지 않는다**

- **결정**: 화면에 나오는 이슈·PR 번호, 저장소 이름, 검색 결과에 링크를 붙인다.
  이슈·PR 주소는 도구가 주지 않으므로 `repo` + 번호로 조립한다
  (`https://github.com/{repo}/issues/{n}` · `/pull/{n}`).
- **막은 것 셋**:
  1. **아무 숫자나 링크하지 않는다.** 이 실행이 실제로 조회한 번호만 링크한다.
     그러지 않으면 카드의 「함수 오류 **47**건」까지 이슈 링크가 된다.
  2. **이슈와 PR 을 구별한다.** `#14` 는 PR 이라 `/pull/14` 로 간다 — `/issues/14` 는 다른 것이다.
  3. **http(s) 가 아닌 주소는 링크로 만들지 않는다.** 도구 출력이 그대로 `href` 가 되는 경로다.
- **픽스처 참조를 어떻게 다루나 — 이 결정의 핵심.**
  픽스처 실행의 `#11`·`#13` 은 **스냅샷 안의 이슈**다. 실제 저장소에는 없을 수 있다
  (확인해 보니 그 저장소의 열린 이슈는 0건이었다). 링크를 없애면 기능이 죽고,
  아무 말 없이 링크하면 화면이 없는 것을 있는 것처럼 말한다. 그래서 **둘 다 한다** —
  링크는 걸되 카드 머리에 「픽스처 스냅샷의 참조다 — 실제 저장소에는 없을 수 있다」를 적고,
  링크마다 같은 내용을 툴팁으로 붙인다.
- **시뮬레이션 이슈는 링크하지 않는다.** fixture 모드에서 승인으로 «만든» 이슈(#9001 등)는
  도구가 스스로 `simulated: true` 를 돌려주는, 확실히 존재하지 않는 대상이다.
  주소 대신 `#9001 (시뮬레이션)` 으로 적는다. live 실행에서는 도구가 준 실제 주소를 쓴다.
- **검색 출처는 카드가 인용하지 않아도 남긴다.** `[ AXES ]` 아래에 이 실행이 받은
  검색 결과의 제목·주소·게시일을 적는다. 트렌드 축의 「N hit」 이 어디서 왔는지
  주소로 확인할 수 있어야 한다. 게시일이 없으면 «게시일 미확인» 으로 적는다 — 0 이나 오늘로 두지 않는다.
- **결정일**: 2026-09-10

## D27. 카드뉴스 굽기는 **사람이 누를 때만** — 에이전트가 알아서 하지 않는다

- **결정**: `export_cardnews` 를 프롬프트의 자동 순서에서 뺐다. 에이전트는 `compose_card` 로
  카드까지 만들고 멈춘다. PNG 로 굽고 ZIP 으로 묶는 일은 화면의 「카드뉴스 내보내기」 버튼이 한다.
- **왜**: 굽기는 **카드당 브라우저를 한 번 띄우는 일**이라 느리다(7장에 십수 초).
  문안을 고칠 때마다 다시 구우면 그만큼이 그대로 낭비다. 그리고 스토리보드가 됐는지는
  사람이 판단할 일이다 — 이 앱의 사람 개입 지점 중 「최종 승인·내보내기」가 바로 여기다.
  자동으로 구우면 그 개입 지점이 이름만 남는다.
- **한 벌만 구현한다**: 굽는 본체를 `mcp-server/src/cards.ts` 의 `exportCardnews()` 로 빼고,
  **도구와 화면 버튼이 같은 함수를 부른다.** 두 곳에 따로 구현하면 한쪽만 고쳐지고,
  사람이 만든 결과물과 에이전트가 만든 결과물이 달라진다.
  경로는 `runDir` 로 받는다 — `config.runsDir` 에 기대면 앱(cwd 가 `app/`)이 엉뚱한 곳을 본다.
- **굽고 난 뒤에만 보여준다**: 내려받기 링크와 미리보기는 디스크에 **실제로 있는 파일**에서만
  나온다. 카드 수와 구워진 PNG 수가 다르면 「카드 7장 중 5장만 구워져 있다」로 알린다.
- **두 번 굽지 않는다**: `cards_exporting` 플래그로 잠근다. 두 번째 요청은 `already_exporting`.
- **검증**: 구워 둔 것을 치우고 버튼으로 다시 만들었더니 PNG 7장이 **바이트까지 같았다**
  (100,342 / 74,105 / 56,017 / 88,623 / 66,497 / 70,988 / 60,831). ZIP 은 `unzip -t` 통과.
- **결정일**: 2026-09-10

## D28. 시작 화면에 실행 모드를 띄운다

- **결정**: 홈 머리에 `LIVE` / `FIXTURE` 배지를 둔다. live 면 호박색이고 대상 저장소를 함께 적는다.
  live 에서는 픽스처 선택칸을 비활성한다 — 서버가 `fixture_in_live_mode` 로 거절하기 때문이다.
- **왜 필요했나**: 화면에 픽스처 선택칸이 늘 보이니 **늘 픽스처인 줄 알기 쉽다.**
  이 결정을 만들면서 확인해 보니 그 순간 `.env.local` 이 `OPS_MODE=live` ·
  `OPS_ALLOW_LIVE_WRITES=1` 이었다 — 시작 화면만 보고는 알 수 없었고,
  거기서 브리핑을 걸고 승인하면 **공개 저장소에 진짜 이슈가 만들어질 상태**였다.
- **승인 게이트에도 적는다**: live 실행의 승인 창은
  「⚠ live 모드다 — 승인하면 진짜 이슈가 만들어진다」를, fixture 는
  「승인해도 실제 이슈는 만들어지지 않는다 (simulated)」를 **승인 전에** 보여준다.
  같은 버튼이 두 가지 다른 결과를 내는데 그 차이가 화면에 없었다.
- **결정일**: 2026-09-10

## D29. 실행 모드는 `.env.local` 을 **매번 다시 읽는다**

- **문제**: `.env.local` 을 `OPS_MODE=fixture` 로 바꿨는데 화면이 계속 `LIVE` 라고 말했다.
  `loadEnvLocal` 은 이미 설정된 환경변수를 덮어쓰지 않으므로, 서버가 뜰 때 들어간
  `process.env.OPS_MODE` 가 **낡은 값**으로 남아 있었다.
- **왜 그냥 둘 수 없나**: 모드를 틀리게 말하는 배지는 없느니만 못하다.
  「FIXTURE」를 보고 안심한 채 브리핑을 걸면 실제 API 를 부르게 된다 — D28 이 막으려던 바로 그 일이다.
- **결정**: `opsMode()` 와 `liveWritesEnabled()` 는 `readEnvLocal()` 로 **파일을 매번 읽는다.**
  화면 표시와 실제 실행이 갈라지지 않게 **childEnv 도 같은 함수**를 쓴다.
  파일에 값이 없을 때만 `process.env` 로 물러난다.
- **파서는 여전히 한 곳에만 있다** — `agent/src/env.ts`. 읽는 방식만 하나 늘렸다.
- **결정일**: 2026-09-10

## D30. 브리핑 기간을 사람이 고른다 · 실행을 지울 수 있다

- **기간**: 시작 화면에서 `최근 1주일` · `최근 15일` · `직접 선택` 중 고른다.
  프리셋은 **누를 때가 아니라 시작할 때** 다시 계산한다 — 화면을 오래 열어 두면 어제 날짜로 도는다.
  직접 선택은 두 날짜 입력의 `min`/`max` 로 역전을 막고, 서버도 형식·순서를 다시 본다.
  뒤집힌 기간을 그대로 넘기면 도구가 빈 결과를 주고 모델은 그것을 「활동 없음」으로 읽는다 —
  그 오해를 만들지 않으려고 두 곳에서 본다.
- **삭제**: 목록에서 `del` → `정말 지운다` 두 번 눌러야 지운다. 4초 뒤 확인 상태가 저절로 풀린다.
  **돌고 있는 실행은 지우지 않는다** (`run_is_live`) — 지우는 동안에도 도구가 그 폴더에 쓰고 있어
  반만 지워진 상태가 남는다.
- **날짜를 보여준다**: 목록 행과 실행 상세 머리에 생성 시각과 기간을 적는다.
  목록에서 넘어오면 언제 만든 브리핑인지 잊기 쉽다. 기간 기록이 없는 옛 실행은
  「기간 기록 없음」으로 적는다 — 오늘 날짜로 채우지 않는다.
- **미리보기는 모달로**: 카드를 넘겨 가며 훑는 일이라 탭을 오갈 필요가 없다.
  `←`/`→` 로 넘기고 `esc` 로 닫는다.
- **결정일**: 2026-09-10

## D31. 모드 판정은 **엔진 두 갈래가 같은 함수**를 쓴다 · 픽스처는 서버에서도 막는다

- **증상**: `.env.local` 을 `OPS_MODE=live` 로 바꾸고 opencode 로 돌렸는데,
  실행 상세가 `run --fixture f2-deploy-fail` · `auth: 기록되지 않음` · `fixture 모드` 라고 적었다.
  화면이 거짓말한 것이 아니었다 — **그 실행은 정말 픽스처였다.** 원인이 셋이었다.
- **① opencode 경로가 모드를 무시했다.** `configContent()` 가 `OPS_MODE: 'fixture'` 를 박아 두어서,
  시작 화면이 `LIVE` 라고 말해도 opencode 실행만 조용히 픽스처로 돌았다.
  → `opsMode()` 를 넘긴다. claude 경로와 **같은 판정 한 곳**을 쓴다.
  쓰기 도구 2개(`ops_create_github_issue`·`ops_revert_issue`)는 그대로 꺼 두므로 live 라도 이 경로는 읽기 전용이다.
- **② 화면이 live 에서도 `fixture_id` 를 보냈다.** 셀렉트를 비활성으로 만들었을 뿐 상태는 남아 있었다.
  → 화면에서 빼고, **서버에서도 다시 막는다** (`mode === 'live'` 면 `fixture_id` 를 무시하고 `note` 로 알린다).
  화면만 막으면 API 를 직접 부를 때 그대로 들어와 실행 기록이 잘못 말한다.
  MCP 서버도 live+fixture 를 `fixture_in_live_mode` 로 거절한다 — 같은 규칙 세 겹.
- **③ opencode 실행에는 `credential_source` 가 없었다.** claude 경로에서만 적고 있었다.
  → `'opencode'` 를 값으로 추가하고 「opencode 자체 인증 (Anthropic 자격증명을 쓰지 않는다)」로 적는다.
  **「기록되지 않음」은 「없음」과 다르다** — 안 적힌 것을 안 적혔다고 말하는 편이 낫다.
- **순환 참조**: `opencode.ts` 가 `runner.ts` 의 `opsMode` 를 필요로 해 `app/lib/mode.ts` 로 떼어냈다.
  `runner.ts` 는 그것을 다시 내보내 호출부는 바뀌지 않는다.
- **확인**: live 로 POST 하니 `note: "live 모드라 fixture_id(f2-deploy-fail)를 무시했다"`,
  기록은 `fixture_id: null · credential_source: opencode`, 자식 프로세스 환경은 `"OPS_MODE":"live"` 에
  `OPS_FIXTURE_ID` 없음. 셋 다 실제로 확인했다.
- **결정일**: 2026-09-10

## D32. 멈춘 실행에 대해 **아는 만큼만, 그러나 빨리** 말한다

- **증상**: opencode 실행 넷이 「종료 코드 null (시그널 SIGTERM)」로만 끝났다. 원인을 알 수 없었다.
- **① 상한을 남의 손에 맡기지 않는다.** `execFile` 의 `timeout` 옵션은 상한에 걸리면 SIGTERM 을 보낸다.
  `close` 에는 시그널만 남아, **상한에 걸린 것인지 밖에서 죽은 것인지 화면이 구별하지 못했다.**
  실패한 실행 셋이 모두 `1200.0s` 정각이었는데도 못 읽었다. → 상한을 직접 재고, 우리가 끊었을 때만 그렇다고 적는다.
- **② 20분을 잠자코 기다리지 않는다.** opencode 로그를 보니 22:52 이후 모든 실행이 `init` 다음에서 멈췄다 —
  세션 생성(`created`)까지 가지 못했다. 모델을 부르기도 전에 부트스트랩에서 선 것이다.
  정상 실행은 실측 1~2초에 첫 줄이 나온다. → **90초 첫 줄 감시**를 둔다.
  20분을 기다려도 0줄이면 그 20분은 아무것도 알려 주지 않는다.
- **아는 것과 모르는 것을 나눠 적는다.** 「무엇을 했는지」(90초 동안 0줄이라 끊었다)와
  「어디를 보면 되는지」(`opencode auth list`, `~/.local/share/opencode/log/opencode.log`)만 적는다.
  **원인을 추측해 적지 않는다** — 무료 등급 모델의 제한으로 보이지만 이 코드가 관측한 사실이 아니다.
- **③ 환경변수 누수는 읽는 자리에서 막는다.** `.env.local` 의 `OPS_FIXTURE_ID` 가 부모 프로세스를 거쳐
  자식 MCP 서버까지 흘러들었다. 부르는 쪽에서 「안 넘긴다」만으로는 막히지 않는다 —
  경로가 셋(claude·opencode·CLI)이라 한 곳만 빠져도 샌다.
  → `config.defaultFixtureId` 가 **live 에서 언제나 null** 을 준다. 빈 문자열도 「없음」으로 본다.
  이것이 D31 의 「세 겹」에 네 번째 겹이다. 회귀 검사 2건을 붙였다.
- **결정일**: 2026-09-10

## D33. 시작·목록 화면 다듬기 — 읽는 순서를 정한다

- **쪽 넘기기**: 지난 실행이 한 쪽에 10건(`RUNS_PAGE`)까지만 나온다. 그 이상은 쌓이기만
  해서 아래 프리셋·저장 항목이 밀려난다. 셈 세 개에는 상태 배지와 같은 색을 준다 —
  표를 읽기 전에 결이 먼저 보인다.
- **색의 뜻을 뒤집는다**: 켜져 있는 쪽이 초록이다. 배지는 「지금 무엇으로 도는가」를 말하므로,
  꺼진(과거·정지) 쪽이 눈에 띄면 거짓말이다. 같은 이유로 시각은 브라우저 현지 시간으로
  그린다 — UTC 문자열이 9시간 어긋나 보였다.
- **질문은 다 고른 뒤 한 번만 보낸다.** 항목마다 보내면 `version` 경쟁이 나고 뒤늦은 답이
  `stale_version` 으로 죽는다. 질문 글의 폭 제한도 뺐다 — 한 항목이 여러 줄로 쪼개지면
  무엇을 고른 건지 알 수 없다.
- **파비콘**은 브라우저 탭의 터미널 `«>_»` 에서 ASCII 게이지로 바꿨다. 화면 주제와 같은 은유다.
- **무료 목록 표기**는 `free` 로 통일한다. 모델명 끝의 `-free`·단가표 0·화면 딱지가 같은 말이어야
  "무료라 골랐는데 과금됐다"는 오해가 없다.
- **결정일**: 2026-09-10
