# 화면 캡처

배포 URL 이 제출 요건에서 빠졌으므로 **캡처가 유일한 동작 증거**다.
각 캡처가 무엇을 증명하는지 여기 적어 둔다. 목록은 `../../RUN.md` 의 캡처 목록과 짝을 맞춘다.

> **캡처 방법은 `CAPTURE_GUIDE.md` 에 있다.** 화면 흐름은 실제 모델 실행으로 전부 검증했고,
> 스크린샷 파일은 사람이 남긴다 — 자동화 도구가 임의 경로에 PNG 를 쓸 수 없었다.

## 캡처 목록

| 파일 | 무엇을 증명하는가 | 채점 |
|---|---|---|
| `01-start.png` | 시작 화면 — 픽스처 선택, 자격증명 출처 표시(구독 로그인) | — |
| `02-running-trace.png` | 진행 화면 + 실행 로그. 도구 이름·시각이 실시간으로 쌓인다 | 4 |
| `03-approval-waiting.png` | **승인 대기** — 무엇을 쓰려는지 펼쳐 본 뒤 승인/거절 | 2 |
| `04-approval-denied.png` | 거절 후 실행이 이어지고 결정 이력에 남는다 | 2 |
| `05-usage-cost.png` | 소요 시간·토큰·비용. **추정값**임과 자격증명 출처를 함께 표시 | 4 |
| `06-charts.png` | 근거 대조를 통과한 SVG 차트 (`source_verified`) | 2 |
| `07-trace-expanded.png` | 실행 로그 펼침 — 도구 입력·출력 **전문** | 4 |
| `08-interrupted-resume.png` | 서버 재시작 후 `interrupted` + 재개/재시도. 걸려 있던 항목은 읽기 전용 | 3 |
| `09-stopped-limit.png` | 종료 조건 발동 화면 | 3 |
| `10-mcp-in-claude-code.png` | Claude Code 에서 이 저장소의 MCP 서버가 붙어 도구 7개가 뜬다 | 5·확장① |

## 캡처하지 않고 기록으로 남긴 것

화면 캡처보다 기록이 더 강한 증거인 항목이 있다.

| 무엇 | 어디에 |
|---|---|
| 승인 없이 쓰기 도구가 거부된 사실 | `../runs/gate-deny/` + SDK `permission_denials` 1건 |
| 승인 후 실행과 되돌리기 | `../runs/gate-approve/approvals.json` (#9001 생성→닫힘) |
| 기준 실행 12시행 + 원고 전문 | `../../eval-results/E0/` |
| 세팅 변화 실험 E4 | `../../eval-results/E4/` |

캡처는 사람이 화면에서 본 것을, 기록은 기계가 남긴 것을 증명한다. 둘을 섞지 않는다.
